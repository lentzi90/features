{
    "name": "KWOK",
    "id": "kwok",
    "version": "0.1.1",
    "description": "KWOK (Kubernetes WithOut Kubelet). This installs both kwok and kwokctl.",
    "options": {
        "version": {
            "type": "string",
            "proposals": [
                "v0.4.0"
            ],
            "default": "v0.5.2",
            "description": "Select or enter kwok version to install."
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
